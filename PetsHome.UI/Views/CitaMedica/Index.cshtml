@model CitaMedicaViewModel
@{
    ViewData["Title"] = "HistorialMedico";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    @* Listado *@
    <div class="card">
        <div class="card-header">
            <h3>Listado de historialmedico</h3>
        </div>

        @* Progress Container - Aparece debajo del header *@
  @*       <div id="datatable-progress-container" class="datatable-progress-container" style="display: none;">
            <div class="d-flex justify-content-between align-items-center">
                <div class="flex-grow-1">
                    <div class="progress mb-2">
                        <div id="datatable-progress-bar" class="progress-bar bg-primary" role="progressbar" style="width: 0%"></div>
                    </div>
                    <div id="datatable-progress-text" class="small text-muted">Cargando datos...</div>
                    <div id="datatable-progress-details" class="small text-danger mt-1" style="display: none;"></div>
                </div>
                <div class="ms-3">
                    <button id="datatable-progress-cancel" class="btn btn-sm btn-outline-secondary">
                        <i class="fas fa-times"></i> Cancelar
                    </button>
                </div>
            </div>
        </div> *@

        <div class="card-body">
            <div class="table-responsive" id="myTableContainer">
                <table class="table table-striped dt-responsive nowrap display" id="datatable">
                    <thead class="text-center bg-primary text-white">
                        <tr>

                            @* <th>Id </th>
                            *@
                            <th>Mascota</th>
                            <th>Fecha Consulta</th>
                             <th>Tipo Consulta</th>
                            <th>Diagnóstico</th>
                            <th>Peso</th>
                             <th>Temperatura</th> 
                            <th>Próxima Cita</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
@* 
<!-- Modal Delete-->
<div class="modal" id="delete-modal" tabindex="-1" role="dialog" data-backdrop="static" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle"></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close" hidden>
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form asp-action="Remove" method="post" autocomplete="off"
                  data-ajax-begin="Catalogs.begin"
                  data-ajax-success="Catalogs.success"
                  data-ajax-failure="Catalogs.failure"
                  data-ajax-complete="Catalogs.complete">
                <input type="hidden" asp-for="medic_Id" id="delete-item-id">
                <div class="modal-body">
                    <div class="row">
                        <div class="col-12">
                            <div class="form-group">
                                <p>¿Está seguro de que desea eliminar este registro?</p>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <input type="submit" class="btn btn-danger" />
                </div>
            </form>
        </div>
    </div>
</div> *@
<!-- Fin Modal -->
@section Scripts {
    <script src="~/js/pages/cita-medica.js"></script>
<script>
        CitaMedica.datatable({
        urlList: "@Url.Action("List")",
        // urlDetail: "@Url.Action("Detail")",
        // urlInsert: "@Url.Action("Create")",
        // urlUpdate: "@Url.Action("Find")"
    });

</script>
    }


