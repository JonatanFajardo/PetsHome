@model RecepcionMercanciaViewModel
@{
    ViewData["Title"] = "Recepciones de Mercancía";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    @* Listado *@
    <div class="card">
        <div class="card-header">
            <h3>Listado de Recepciones de Mercancía</h3>
        </div>

        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped dt-responsive nowrap" id="datatable">
                    <thead class="text-center bg-primary text-white">
                        <tr>
                            <th>Id</th>
                            <th>Fecha</th>
                            <th>Tipo Recepción</th>
                            <th>Número Documento</th>
                            <th>Refugio</th>
                            <th>Usuario Creación</th>
                            <th>Fecha Creación</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>

    <!-- Modal Delete-->
    <div class="modal" id="delete-modal" tabindex="-1" role="dialog" data-backdrop="static" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Eliminar Recepción</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close" hidden>
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <form asp-action="Delete" method="post" autocomplete="off"
                      data-ajax-begin="Catalogs.begin"
                      data-ajax-success="Catalogs.success"
                      data-ajax-failure="Catalogs.failure"
                      data-ajax-complete="Catalogs.complete">
                    <input type="hidden" asp-for="recep_Id" id="delete-item-id">
                    <div class="modal-body">
                        <div class="row">
                            <div class="col-12">
                                <div class="form-group">
                                    <p>¿Está seguro de que desea eliminar esta recepción de mercancía?</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                        <input type="submit" class="btn btn-danger" value="Eliminar" />
                    </div>
                </form>
            </div>
        </div>
    </div>
    <!-- Fin Modal -->
</div>
@section Scripts {
<script src="~/js/pages/recepcionmercancia.js"></script>
<script>
    RecepcionMercancia.datatable({
        urlList: "@Url.Action("List")",
        urlDetail: "@Url.Action("Detail")",
        urlInsert: "@Url.Action("EditRecepciones")",
        urlUpdate: "@Url.Action("EditRecepciones")"
    });
</script>
}